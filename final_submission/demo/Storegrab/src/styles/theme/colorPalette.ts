import {palette} from '../variables.json'

const ColorPalette = {
  backgroundDefault: palette['background-default'],
  backgroundSecondary: palette['background-secondary'],
  backgroundBlue: palette['background-blue'],

  textDarken: palette['text-darken'],
  textDefault: palette['text-default'],
  textSecondary: palette['text-secondary'],

  primary: palette.primary,
  primaryLight: palette['primary-light'],
  primarySuperLight: palette['primary-super-light'],

  success: palette.success,

  alertRed: palette['alert-red'],
  error: palette.error,

  coreMarkGreen: palette['core-mark-green'],
  coreMarkOrange: palette['core-mark-orange'],

  black: palette.black,
  solidBlack: palette['solid-black'],
  brightWhite: palette.white,
  blueDark: palette['blue-dark'],

  lightGrey: palette['light-grey']
};
export default ColorPalette;
